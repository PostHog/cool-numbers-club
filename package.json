{
  "name": "cool-numbers-club",
  "version": "1.0.0",
  "private": true,
  "description": "A register of cool pull request numbers in posthog/posthog.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "fetch": "node scripts/fetch.mjs",
    "refresh": "npm run fetch && npm run build",
    "dev": "npm run build && npx --yes serve dist",
    "og": "node scripts/og.mjs"
  }
}
