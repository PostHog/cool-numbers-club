{
  "name": "cool-numbers-club",
  "version": "1.0.0",
  "private": true,
  "description": "A register of cool pull request numbers in posthog/posthog.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "fetch": "node scripts/fetch.mjs",
    "render": "node scripts/build.mjs",
    "build": "npm run fetch && npm run render",
    "dev": "npm run render && npx --yes serve dist"
  }
}
